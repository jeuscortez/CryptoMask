<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_image_options"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="#072059"
    tools:context="com.example.cortez.cryptomask.ImageOptionsActivity">

    <Button
        android:text="Mask Image"
        android:layout_width="150dp"
        android:layout_height="50dp"
        android:layout_marginTop="52dp"
        android:background="#33e5d9"
        android:id="@+id/EncryptImage"
        android:textSize="14sp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:text="Un-Mask"
        android:layout_width="150dp"
        android:layout_height="50dp"
        android:layout_below="@+id/EncryptImage"
        android:layout_alignLeft="@+id/EncryptImage"
        android:layout_alignStart="@+id/EncryptImage"
        android:layout_marginTop="103dp"
        android:background="#33e5d9"
        android:id="@+id/DecryptImage"
        android:textSize="14sp" />

        <Button
            android:text="Delete Masked Image"
            android:layout_width="150dp"
            android:layout_height="50dp"
            android:layout_below="@+id/DecryptImage"
            android:layout_alignLeft="@+id/DecryptImage"
            android:layout_alignStart="@+id/DecryptImage"
            android:layout_marginTop="103dp"
            android:background="#33e5d9"
            android:id="@+id/DeleteImageEncryption"
            android:textSize="14sp" />

</RelativeLayout>
